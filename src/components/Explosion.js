import Assets from "../core/AssetManager";
import Scene from "./Scene";


function createExplosion(){
    let ssheet = new PIXI.BaseTexture.from(Assets._assets.explosion.png);

    explosionSheet['explode'] = [
    ]
}